题目简述：

> 给出一个字符串 `s`（仅含有小写英文字母和括号）。
>
> 请你按照从括号内到外的顺序，逐层反转每对匹配括号中的字符串，并返回最终的结果。
>
>  注意，您的结果中 **不应** 包含任何括号。

题目链接：[1190. 反转每对括号间的子串](https://leetcode.cn/problems/reverse-substrings-between-each-pair-of-parentheses/)

# 模拟

## 思路

暴力模拟的思路感觉没啥好说的啊，需要 LIFO 的操作顺序，很明显就用栈或递归了。

碰到左括号就压入

比如：对于 `ab(cd(ef))`，顺序扫描，

- 遇到左括号就将左括号后子串压入栈，直到再次碰到括号便结算；
- 遇到右括号就结算，弹出栈，反转，检查栈内是否有子串，若有则拼接栈顶子串，否则直接与结果串拼接。

---

这个方案不是最优的。后文将介绍更好的方案。

## 代码

算法的最坏时间复杂度为 $O(n^2)$，空间复杂度为 $O(n)$。

```java
class Solution {
    public String reverseParentheses(String s) {
        Deque<StringBuilder> stack = new ArrayDeque<>();
        StringBuilder sb = new StringBuilder();

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);

            if (c == '(') {
                stack.addLast(sb);
                sb = new StringBuilder();
            } else if (c == ')') {
                sb.reverse();
                StringBuilder tmp = stack.removeLast();
                prev.append(sb);
                sb = tmp;
            } else {
                sb.append(c);
            }
        }

        return sb.toString();
    }
}
```

# 反转访问顺序

## 思路

模拟的方案还是太重了，因为在遍历的过程中真的在不断地反转子串，其中不断地反转子串是导致算法最坏时间复杂度达到 $O(n^2)$ 的根源——尽管算法的平均时间复杂度接近 $O(n)$。

实际上，我们有更轻巧的方案：只反转遍历的顺序，不必在遍历中不断地反转子串。

首先预处理每对括号的匹配位置。遍历字符串时，正常情况下从左向右访问；遇到括号时，跳转到与其匹配的括号，并将访问方向反转。

例如，对于 `ab(cd(ef))`，访问到内层 `(` 时跳转到对应的 `)`，之后从右向左访问 `fe`，再遇到外层 `)` 时再次跳转并反转方向。这样就相当于完成了括号内字符串的反转，而实际上并没有进行任何字符串反转。

遍历过程中只将遇到的字母加入结果，因此最终结果中不会包含括号。

## 代码

算法的时间复杂度严格为 $O(n)$，空间复杂度为 $O(n)$。

```java
class Solution {
    public String reverseParentheses(String s) {
        int n = s.length();
        int[] pair = new int[n];
        Deque<Integer> stack = new ArrayDeque<>();

        for (int i = 0; i < n; i++) {
            if (s.charAt(i) == '(') {
                stack.addLast(i);
            } else if (s.charAt(i) == ')') {
                int j = stack.removeLast();
                pair[i] = j;
                pair[j] = i;
            }
        }

        StringBuilder res = new StringBuilder();
        int i = 0;
        int step = 1;

        while (i >= 0 && i < n) {
            char c = s.charAt(i);
            if (c == '(' || c == ')') {
                i = pair[i];
                step = -step;
            } else {
                res.append(c);
            }
            i += step;
        }

        return res.toString();
    }
}
```
