题目简述：

> 在两条独立的水平线上按给定的顺序写下 `nums1` 和 `nums2` 中的整数。
>
> 现在，可以绘制一些连接两个数字 `nums1[i]` 和 `nums2[j]` 的直线，这些直线需要同时满足：
>
> -  `nums1[i] == nums2[j]`
> - 且绘制的直线不与任何其他连线（非水平线）相交。
>
> 请注意，连线即使在端点也不能相交：每个数字只能属于一条连线。
>
> 以这种方法绘制线条，并返回可以绘制的最大连线数。
>
> **提示：**
>
> - `1 <= nums1.length, nums2.length <= 500`
> - `1 <= nums1[i], nums2[j] <= 2000`

题目链接：[1035. 不相交的线](https://leetcode.cn/problems/uncrossed-lines/)

# 思路

这个问题我一眼就看出来子问题解之间的递归依赖关系，存在最优子结构，因此考虑动态规划。从题目给定的数据范围看，也很像是需要设计 $O(nm)$ 时间复杂度的算法。我们有两个数组，自然想到设计双状态的状态值。

定义 $dp[i][j]$ 表示将 $nums1[i]$ 与 $nums2[j]$ 尝试相连且不考虑 $nums1[i]$ 与 $nums2[j]$ 以后的元素，有
$$
dp[i][j]=\left\{\begin{aligned}&\max\big\{dp[i-1][j],\,dp[i][j-1]\big\},&&nums1[i]\neq nums2[j]\\\\&dp[i-1][j-1]+1,&&nums1[i]=nums2[j]\end{aligned}\right.
$$
最后，$dp[n][m]$ 即为问题的解。

这样设计状态的好处是显而易见的：如果 $nums1[i]$ 与 $nums2[j]$ 能够相连，我们所依赖的子问题的解 $dp[i-1][j-1]$ 必然不涉及 $nums1[i]$ 和 $nums2[j]$，因此子问题中必然不存在与 $nums1[i]$ 与 $nums2[j]$ 的连线相交的线。如果 $nums1[i]$ 与 $nums2[j]$ 不能相连，也是同理的。我们从结构上便确保了不可能存在相交的线。

# 代码

算法的时间复杂度为 $O(nm)$，空间复杂度 $O(nm)$。

```java
class Solution {
    public int maxUncrossedLines(int[] nums1, int[] nums2) {
        int n = nums1.length;
        int m = nums2.length;
        
        int[][] dp = new int[n + 1][m + 1];
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                dp[i][j] = (nums1[i - 1] == nums2[j - 1]) ? dp[i - 1][j - 1] + 1 : Math.max(dp[i - 1][j], dp[i][j - 1]);
            }
        }

        return dp[n][m];
    }
}
```

利用滚动数组降低空间开销后，空间复杂度在理论上来到 $O(\min\{n,m\})$。

```java
class Solution {
    public int maxUncrossedLines(int[] nums1, int[] nums2) {
        int n = nums1.length;
        int m = nums2.length;
        
        int[] dp1 = new int[m + 1];
        int[] dp2 = new int[m + 1];
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                dp2[j] = (nums1[i - 1] == nums2[j - 1]) ? dp1[j - 1] + 1 : Math.max(dp1[j], dp2[j - 1]);
            }
            int[] tmp = dp2;
            dp2 = dp1;
            dp1 = tmp;
        }

        return dp1[m];
    }
}
```

