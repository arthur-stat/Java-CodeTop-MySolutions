题目简述：

> 给定一个字符串 `s`，计算 `s` 的 **不同非空子序列** 的个数。因为结果可能很大，所以返回答案需要对 **`10^9 + 7` 取余** 。
>
> 字符串的 **子序列** 是经由原字符串删除一些（也可能不删除）字符但不改变剩余字符相对位置的一个新字符串。
>
> - 例如，`"ace"` 是 `"***a***b***c***d***e***"` 的一个子序列，但 `"aec"` 不是。
>
> **提示：**
>
> - `1 <= s.length <= 2000`
> - `s` 仅由小写英文字母组成

题目链接：[940. 不同的子序列 II](https://leetcode.cn/problems/distinct-subsequences-ii/)

# 思路

在题目的数据范围下，考虑回溯算法找出所有非空子序列必然是不现实的。

子序列问题常考虑动态规划。记 $f(i)$ 表示以第 $i$ 个元素结尾的不重复子序列个数、$s[i]$ 为第 $i$ 个元素的值，若第 $i$ 个元素与此前的元素均不重复值，则易见有
$$
f(i)=\sum^{i-1}_{j=1}f(j)+1
$$
但当存在重复呢？假设存在 $j$ 使得 $s[i]=s[j]$，则可以肯定，对于 $s$ 的长为 $j-1$ 的前缀子串所产生的所有不重复子排列，下一个后继元素不管考虑 $s[i]$ 还是 $s[j]$ 都没有区别，因为无论后继元素选择哪一个，产生的新序列集合都是完全相等的。

因此，假设 $j'$ 满足 $j'<i$ 且 $\forall k\in(j',k)$ 都有 $s[k]\neq s[i]$，即 $j'$ 是索引 $i$ 以前最后一个与其元素值重复的元素之索引，则我们将前缀子串在 $j'$ 处一分为二，那么

- $s$ 的长度为 $j'-1$ 的前缀所产生的所有不重复子序列，与 $s[i]$ 拼接后产生的所有新序列的数量为 $f(j')$，但它们与 $f[j']$ 本身所对应的所有序列完全重复，因此对 $f[i]$ 的贡献为 $0$；
- $j'$ 及以后的元素与 $s[i]$ 均不重复，可直接计算贡献 $\sum\limits^{i-1}_{k=j'}f(k)$，这相当于对以 $s[k]$ 结尾的所有子排列均插入后继元素 $s[i]$ 并累积计算新子排列数。

综上，有
$$
f(i)=0+\sum^{i-1}_{k=j'}f(k)=\sum^{i-1}_{k=j'}f(k)
$$

最后，$\sum f(i)$ 即为问题的解。

# 代码

算法的时间复杂度为 $O(n)$，空间复杂度为 $O(1)$。

```java
class Solution {

    public static int m = 1_000_000_007;

    public int distinctSubseqII(String s) {
        int[] lastIdx = new int[26];
        Arrays.fill(lastIdx, -1);

        int n = s.length();
        int[] dp = new int[n + 1];
        int[] sum = new int[n + 1];
        dp[0] = 0;
        sum[0] = 0;

        for (int i = 1; i <= n; i++) {
            int c = s.charAt(i - 1) - 'a';
            dp[i] = (lastIdx[c] == -1) ? sum[i - 1] + 1 : sum[i - 1] - sum[lastIdx[c] - 1];
            sum[i] = mod(dp[i] + sum[i - 1]);
            lastIdx[c] = i;
        }

        return sum[n];
    }

    public static int mod(int x) {
        int y = x % m;
        return y < 0 ? y + m : y;
    }
}
```

