题目简述：

> 树是一个无向图，其中任何两个顶点只通过一条路径连接。 换句话说，任何一个没有简单环路的连通图都是一棵树。
>
> 给你一棵包含 `n` 个节点的树，标记为 `0` 到 `n - 1` 。给定数字 `n` 和一个有 `n - 1` 条无向边的 `edges` 列表（每一个边都是一对标签），其中 `edges[i] = [ai, bi]` 表示树中节点 `ai` 和 `bi` 之间存在一条无向边。
>
> 可选择树中任何一个节点作为根。当选择节点 `x` 作为根节点时，设结果树的高度为 `h` 。在所有可能的树中，具有最小高度的树（即，`min(h)`）被称为 **最小高度树** 。
>
> 请你找到所有的 **最小高度树** 并按 **任意顺序** 返回它们的根节点标签列表。
>
> 树的 **高度** 是指根节点和叶子节点之间最长向下路径上边的数量。

题目链接：[310. 最小高度树](https://leetcode.cn/problems/minimum-height-trees/)

# 思路

如果我们把树摊开（向四周水平展开）并将其看作无向图，则不应再将根节点视为「树的根节点」，而应当将其视为无向图的中心。

注意，这意味着，在脑中想象该无向图时，不要把根节点置于最上方。因为图是无向的，故应将根节点置于图的中心。这一点至关重要，会影响到对算法的直观理解。

众所周知的是，对于有向的 DAG，如果需要拓扑排序，我们可以考虑 Kahn 算法。其思路是计算入度，然后将所有入度为 0 的节点压入队列，进行一轮 BFS。在该轮 BFS 中，节点从队列出队后，我们令该节点所指向的所有剩余节点入度减一（即移除该节点，更新剩余节点入度）。该轮 BFS 结束后，我们统计剩余节点中入度为 0 的节点，再将这些节点压入队列，然后进行下一轮 BFS。

此处同样本质上是拓扑排序，但由于是无向图，所以需要将 Kahn 算法中的「入度」替换为「度」，剩余节点的入队条件由「入度为 0」改为「度为 1」。这很好理解，因为「度」同时包含了入度与出度，度为 0 可以视为 入度为 1——如果度为 0，意味着该节点完全是孤立的，这说明输入并不是一棵树。并且，最终所进行的 BFS 轮数近似于树的最小高度（至于为什么是近似，请见下文分解）。

在借鉴 DAG 的 Kahn 算法以应用在本问题的无向图拓扑排序中，最关键的一点是 BFS 终止条件的选择。终止条件是**剩余节点数为 1 或 2**。下面我们说明为什么此条件为合法的终止条件，而不是其他的条件。

前文提到，对于本问题，树的根节点应被视为无向图的中心。那么，对于以该中心为起点的最长的两条边（即考虑深度最深的两个叶子节点，取从根节点到达这两个叶子节点的路径，可以视为该无向图的「直径」），相连后是一条链。该链上如果有奇数个节点，那么毫无疑问树只有一个根节点，因为链的中心唯一对应一个节点；该链上如果有偶数个节点，则该链的中心便不再精确地位于某个节点上，此时就存在两个可能的根节点，分别位于链中心的左右两侧。

仿 Kahn 算法的过程，就是从叶子节点开始、不断地剥离叶子并向根节点「逼近」的过程。

在实际的算法实现中，无需事先判断可能的根节点数量是 1 还是 2，只需要设置终止条件为「剩余节点数为 1 或 2」即可。这是因为，每一轮剥离叶子时我们都剥离了所有边的叶子，因此我们也可以逆着时间给所有边把被剥离的叶子接回去，故可以考虑最简情况，要么只有一个中心节点，要么除中心节点外还剩下一个节点（此时剩下的那个节点就是另一个合法的根节点）——剥离的结果只可能有这两种结果，而且**这两种结果是互斥的**。

> 如果一共剩下三个节点呢？那意味着还可以剥，三个节点中必然存在一个唯一的中心节点。这不是最简情况。当然，该结论成立的前提是剩余图始终保持连通且仍然是一棵树，这是最本质的原因。

这也阐明了，如果层层剥离后的最简情况下只有一个节点，则 BFS 的轮数就是树的最小高度；如果层层剥离后的最简情况下有两个节点，即存在两个可能的根节点，则 BFS 的轮数就是树的最小高度减一。不过本题不要求计算树高，因此不需要分类讨论。

# 代码

算法的时间复杂度为 $O(n)$，空间复杂度为 $O(n)$。

```java
class Solution {
    public List<Integer> findMinHeightTrees(int n, int[][] edges) {
        // 初始化度表与邻接表
        int[] degree = new int[n];
        List<List<Integer>> adj = new ArrayList<>(n);
        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());
        for (int[] edge : edges) {
            degree[edge[0]]++;
            degree[edge[1]]++;
            adj.get(edge[0]).add(edge[1]);
            adj.get(edge[1]).add(edge[0]);
        }
        Deque<Integer> q = new ArrayDeque<>();

        // 将度为 1 的节点压入初始队列
        for (int i = 0; i < n; i++) if (degree[i] == 1) q.offer(i);

        // 类似 Kahn 算法的变体 BFS 算法，但针对无向图
        Set<Integer> remain = IntStream.range(0, n).collect(HashSet::new, HashSet::add, HashSet::addAll);
        int num = q.size();
        while (remain.size() > 2) {
            for (int i = 0; i < num; i++) {
                int node = q.poll();
                remain.remove(node);
                List<Integer> next = adj.get(node);
                for (int other : next) {
                    if (degree[other] > 1) {
                        degree[other]--;
                        if (degree[other] == 1) q.offer(other);
                    }
                }
            }
            num = q.size();
        }

        return new ArrayList<>(remain);
    }
}
```

