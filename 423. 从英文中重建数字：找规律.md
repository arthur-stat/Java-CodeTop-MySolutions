题目简述：

> 给你一个字符串 `s` ，其中包含字母顺序打乱的用英文单词表示的若干数字（`0-9`）。按 **升序** 返回原始的数字。

题目链接：[423. 从英文中重建数字](https://leetcode.cn/problems/reconstruct-original-digits-from-english/)

# 思路

找规律的问题。观察：

- zero
- one
- two
- three
- four
- five
- six
- seven
- eight
- nine

这些数字有下述特点：

- 只有 zero 含有 `'z'`
- 只有 two 含有 `'w'`
- 只有 four 含有 `'u'`
- 只有 six 含有 `'x'`
- 只有 eight 含有 `'g'`

因此，靠 z、w、u、x、g，便能唯一地确定 zero、two、four、six、eight。于是，只剩下 one、three、five、seven、nine 待确认。而在这些剩下的单词中，又只有 three 含有 `'r'`、只有 five 含有 `'f'`、只有 seven 含有 `'s'`，故 three、five、seven 也能被唯一确认。

最后，只剩下 one 与 nine——二者中只有 nine 含有 `'i'`，于是，最终我们能够确认所有单词的数量。只需要按上述原理编写对应的规则即可。

---

实际上，我们可以将上述规律整理为数字表达式。如果我们用 $|\alpha|$ 表示字母或对应的数字 $\alpha$ 出现的频次，则有
$$
\left\{\begin{aligned}
&|0|=|z|\\\\
&|1|=|e|-|0|-|8|-2\cdot|3|-|5|-2\cdot|7|-|9|\\\\
&|2|=|w|\\\\
&|3|=|r|-|0|-|4|\\\\
&|4|=|u|\\\\
&|5|=|f|-|4|\\\\
&|6|=|x|\\\\
&|7|=|s|-|6|\\\\
&|8|=|g|\\\\
&|9|=|i|-|6|-|8|-|5|
\end{aligned}\right.
$$


# 代码

算法的时间复杂度为 $O(n)$，空间复杂度为 $O(1)$。

```java
class Solution {
    public String originalDigits(String s) {
        int[] res = new int[10];
        int[] count = new int[10];  // 分别用于计数 z, t, u, x, g, r, f, s, i, e 的数量
        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (c == 'z') {
                res[0]++;
                count[0]++;
            } else if (c == 'w') {
                res[2]++;
                count[1]++;
            } else if (c == 'u') {
                res[4]++;
                count[2]++;
            } else if (c == 'x') {
                res[6]++;
                count[3]++;
            } else if (c == 'g') {
                res[8]++;
                count[4]++;
            } else if (c == 'r') {
                count[5]++;
            } else if (c == 'f') {
                count[6]++;
            } else if (c == 's') {
                count[7]++;
            } else if (c == 'i') {
                count[8]++;
            } else if (c == 'e') {
                count[9]++;
            }
        }

        res[3] = count[5] - res[0] - res[4];
        res[5] = count[6] - res[4];
        res[7] = count[7] - res[6];
        res[9] = count[8] - res[6] - res[8] - res[5];
        res[1] = count[9] - res[0] - res[8] - 2 * res[3] - res[5] - 2 * res[7] - res[9];

        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < 10; i++) {
            for (int j = 0; j < res[i]; j++) sb.append(i);
        }
        return sb.toString();
    }
}
```

