题目简述：

> 给你一个整数数组 `citations` ，其中 `citations[i]` 表示研究者的第 `i` 篇论文被引用的次数。计算并返回该研究者的 **`h` 指数**。
>
> 根据维基百科上 h 指数的定义：`h` 代表“高引用次数” ，一名科研人员的 `h` **指数** 是指他（她）至少发表了 `h` 篇论文，并且 **至少** 有 `h` 篇论文被引用次数大于等于 `h` 。如果 `h` 有多种可能的值，**`h` 指数** 是其中最大的那个。

题目链接：[274. H 指数](https://leetcode.cn/problems/h-index/)

# 二分答案

## 思路

敏锐地察觉到此处值存在单调性：如果 `k` 是合法的 h 指数候选值，即该科研人员至少有 `k` 篇论文的被引次数大于等于 `k`，则 `k-1` 也是合法的 h 指数候选值，因为该科研人员必然同时有 `k-1` 篇论文的被引次数大于等于 `k-1`。

所以考虑值域二分即可，即二分答案。

---

怎么理解值的单调性呢？想象对于输入 `x`，我们有映射 `y=f(x)`，`y` 随 `x` 具有单调性。对于本问题，输入 `x` 即可能的 h 指数，`y` 是一个二元的布尔值，表示 `x` 是否是合法的 h 指数候选值。由上述分析，`y` 具有单调性，一定存在 `x0`，使得任何小于等于 `x0` 的数 `z` 都有 `f(z)` 为 `true`，且任何大于 `x0` 的数 `t` 都有 `f(t)` 为 `false`，于是 `x0` 即为 h 指数，即最大的候选值。

于是，我们对 `x` 进行二分查找，但以 `y` 作为收缩二分边界的判据。

## 代码

二分答案算法本身的时间复杂度为 $O(\log n)$、空间复杂度为 $O(1)$，但下面的代码实现中 `f` 我们是通过快速排序后直接按「逆序第 `x` 篇论文的被引数是否大于等于 `x`」⇔「是否至少有 `x` 篇论文的被引数大于等于 `x`」的逻辑判断值的，因此总的时间复杂度来到 $O(n\log n)$。

下文会介绍 $O(n)$ 时间复杂度的更优算法。

```java
class Solution {
    public int hIndex(int[] citations) {
        int l = 0;
        int r = citations.length - 1;
        Arrays.sort(citations);

        while (l <= r) {
            int mid = l + (r - l) / 2;
            if (f(mid, citations)) l = mid + 1;
            else r = mid - 1;
        }

        return r + 1;
    }

    public boolean f(int h, int[] citations) {
        int n = citations.length;
        return citations[n - h - 1] >= h + 1;
    }
}
```

# 计数桶

## 思路

实际上，我们根本不需要付出 $O(n\log n)$ 的时间开销进行排序——甚至不需要二分查找，即使观察到单调性而考虑的二分是优雅的。

由于一共有 `n` 篇论文，因此我们划分出 `n` 个桶，第 `n` 个桶表示被引数大于等于 `n` 的论文数量。线性地遍历一次数组，在遍历中更新计数桶。最后，桶内元素数量大于等于桶编号的计数桶中编号最大的那个计数桶的编号即为 h 指数。

在实现计数桶时，可以借鉴桶排序的频数统计代码实现，通过「遍历计数 + 后续倾倒」的方式，避免在每次遍历计数时也遍历所有桶以更新全部计数桶。

## 代码

算法的时间复杂度为 $O(n)$，空间复杂度为 $O(n)$。

```java
class Solution {
    public int hIndex(int[] citations) {
        int n = citations.length;
        int[] bucket = new int[n + 1];
        for (int c : citations) bucket[Math.min(c, n)]++;
        for (int i = n; i > 0; i--) {
            if (bucket[i] >= i) return i;
            bucket[i - 1] += bucket[i];
        }
        return 0;
    }
}
```

