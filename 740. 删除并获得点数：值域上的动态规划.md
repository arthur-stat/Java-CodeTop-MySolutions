题目简述：

> 给你一个整数数组 `nums` ，你可以对它进行一些操作。
>
> 每次操作中，选择任意一个 `nums[i]` ，删除它并获得 `nums[i]` 的点数。之后，你必须删除 **所有** 等于 `nums[i] - 1` 和 `nums[i] + 1` 的元素。
>
> 开始你拥有 `0` 个点数。返回你能通过这些操作获得的最大点数。
>
> **提示：**
>
> - `1 <= nums.length <= 2 * 10^4`
> - `1 <= nums[i] <= 10^4`

题目链接：[740. 删除并获得点数](https://leetcode.cn/problems/delete-and-earn/)

# 思路

观察问题，注意到该问题与元素间的位置关系是完全无关的，因此我们可以将 `nums` 转为值统计。本问题有以下特征：

- 一旦 `nums[i]` 被删除并获得积分，所有与 `nums[i]` 同值的元素都可以被删除并获得积分，且不会有任何影响，因此只要删除 `nums[i]` 且获得积分，就能删除与 `nums[i]` 同值的元素并获得积分；
- 一旦删除 `nums[i]` 且获得积分，就不能再通过所有值为 `nums[i] - 1` 与 `nums[i] + 1` 的元素获得积分。

这不就是典型的动态规划问题吗？比如 [198. 打家劫舍](https://leetcode.cn/problems/house-robber/)，与本问题完全是同一个数学模型。

注意到数据范围，可以设计新数组 `values`，索引表示 `nums` 中可能的值，其值表示对应值在 `nums` 中的频数。这样，定义 $dp[i]$ 表示只可能删除值小于等于 `i` 的元素时能获得的最大点数，有
$$
dp[i]=\max\big\{dp[i-1],\ dp[i-2]+i\times values[i]\big\}
$$

最后 $dp$ 数组的最后一个值即为问题的解。

# 代码

算法的时间复杂度为 $O(n)$，空间复杂度为 $O(n)$。

```java
class Solution {

    public static int size = 10_000;

    public int deleteAndEarn(int[] nums) {
        int n = nums.length;
        int[] values = new int[size + 1];
        for (int num : nums) values[num]++;

        int[] dp = new int[size + 1];
        if (size > 0) dp[1] = values[1];
        for (int v = 2; v <= size; v++) {
            dp[v] = Math.max(dp[v - 1], dp[v - 2] + v * values[v]);
        }

        return dp[size];
    }
}
```

