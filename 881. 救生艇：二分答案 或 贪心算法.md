题目简述：

> 给定数组 `people` 。`people[i]`表示第 `i` 个人的体重 ，**船的数量不限**，每艘船可以承载的最大重量为 `limit`。
>
> 每艘船最多可同时载两人，但条件是这些人的重量之和最多为 `limit`。
>
> 返回 *承载所有人所需的最小船数* 。

题目链接：[881. 救生艇](https://leetcode.cn/problems/boats-to-save-people/)

# 二分答案

## 思路

显然地，

- 我们有能力判断确定数量的船能否载上所有人，只需要对体重数组排序后考虑双指针算法，优先载上体重更大的人，再判断能否顺带捎上当前剩余体重最小的人，这样的分配方案可以确保使用最小有效数量的船；
- 船的数量能否载上所有人，这一子问题具有**单调性**，必然存在一个数，当船数量小于该数时无法载上所有人，而当船数量大于等于该数时可以载上所有人。

利用单调性，考虑二分查找，就可以应用二分答案算法。

## 代码

算法的时间复杂度为 $O(n\log n)$，空间复杂度为 $O(\log n)$。

```java
class Solution {
    // 二分查找确定最少所需救生艇数量
    public int numRescueBoats(int[] people, int limit) {
        Arrays.sort(people);
        int l = 0;
        int r = people.length - 1;
        while (l <= r) {
            int mid = l + (r - l) / 2;
            if (!canSaveAll(people, limit, mid)) {
                l = mid + 1;
            } else {
                r = mid - 1;
            }
        }
        return l;
    }
    
    // 双指针判断指定救生艇数量能否救出所有人
    public boolean canSaveAll(int[] people, int limit, int boats) {
        int i = 0;
        int j = people.length - 1;
        while (i <= j) {
            int rest = limit - people[j];
            j--;
            boats--;
            if (boats < 0) return false;
            if (people[i] <= rest && i <= j) i++;
        }
        return true;
    }
}
```

# 贪心算法

## 思路

其实排序后，我们不光有能力判断确定数量的船能否载上所有人，还能一次性就直接数出来需要多少船，因为前文有提到，「这样的分配方案可以确保使用最小有效数量的船」。

所以二分答案本质绕了一点弯路，但就时空复杂度数量级而言，二分答案和贪心算法数相同的，不过贪心算法的常数时间复杂度更小。

## 代码

算法的时间复杂度为 $O(n\log n)$，空间复杂度为 $O(\log n)$。

```java
class Solution {
    public int numRescueBoats(int[] people, int limit) {
        Arrays.sort(people);
        int i = 0;
        int j = people.length - 1;
        int boats = 0;

        while (i <= j) {
            int rest = limit - people[j];
            j--;
            boats++;
            if (people[i] <= rest && i <= j) i++;
        }
        return boats;
    }
}
```

