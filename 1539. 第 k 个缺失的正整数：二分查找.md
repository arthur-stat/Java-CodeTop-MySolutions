题目简述：

> 给你一个 **严格升序排列** 的正整数数组 `arr` 和一个整数 `k` 。
>
> 请你找到这个数组里第 `k` 个缺失的正整数。
>
> **进阶：**
>
> 你可以设计一个时间复杂度小于 O(n) 的算法解决此问题吗？

题目链接：[1539. 第 k 个缺失的正整数](https://leetcode.cn/problems/kth-missing-positive-number/)

# 基本思路

对问题建模。假设 `arr` 的元素为 $[a_1,a_2,\cdots,a_n]$，则

- $(0,a_1)$ 内共有 $a_1-0-1$ 个缺失的正整数
- $(a_1,a_2)$ 内共有 $a_2-a_1 - 1$ 个缺失的正整数
- $(a_2,a_3)$ 内共有 $a_3-a_2 - 1$ 个缺失的正整数
- ……

只要累积

# 基本代码

算法的最坏时间复杂度为 $O(n)$，空间复杂度为 $O(1)$。

```java
class Solution {
    public int findKthPositive(int[] arr, int k) {
        int prev = 0;
        int i = 0;
        int count = 0;
        while (true) {
            if (i < arr.length && count + arr[i] - prev - 1 < k) {
                count += arr[i] - prev - 1;
                prev = arr[i++];
            } else {
                return prev + k - count;
            }
        }
    }
}
```

# 结合二分查找的思路

可以结合二分查找快速确定满足 `count + arr[i] - prev - 1 >= k` 的最小 `i`，但不必不断累加 `count`。

我们能肯定，对于 $a_n$，小于 $a_n$ 的缺失正整数必然有 $a_n-n$ 个，且缺失的正整数随索引而单调递增。借助此规律，可以设计二分查找算法。

# 结合二分查找的代码

算法的时间复杂度为 $O(\log n)$，空间复杂度为 $O(1)$。

注意处理 $a_1>k$ 的情况，即 `r == -1`。

```java
class Solution {
    public int findKthPositive(int[] arr, int k) {
        int l = 0;
        int r = arr.length - 1;
        while (l <= r) {
            int mid = l + (r - l) / 2;
            if (arr[mid] - mid - 1 < k) l = mid + 1;
            else r = mid - 1;
        }
        return r == -1 ? k : arr[r] + (k - (arr[r] - r - 1));
    }
}
```
