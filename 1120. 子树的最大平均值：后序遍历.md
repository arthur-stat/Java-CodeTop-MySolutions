题目简述：

> 给定一棵二叉树的根节点 `root`，找出所有子树中，平均值最大的那个，返回这个最大平均值。

题目链接：[1120. 子树的最大平均值](https://leetcode.cn/problems/maximum-average-subtree/description/)

# 思路

考虑递归实现后序遍历的话是很容易的。

设计递归体返回数组，数组的第一个值记录以该节点为根节点的子树节点值之和，第二个值记录以该节点为根节点的子树节点个数，然后在后序遍历的过程中利用递归返回值更新已知的最大平均子树值。最终，后序遍历完成时，我们就得到了子树的最大平均值。

# 代码

```java
class Solution {

    private double res;

    public double maximumAverageSubtree(TreeNode root) {
        TreeNode dummy = new TreeNode(0, root, null);
        res = Double.NEGATIVE_INFINITY;
        dfs(dummy);
        return res;
    }

    private double[] dfs(TreeNode root) {
        if (root == null) return new double[]{.0, .0};
        double[] l = dfs(root.left);
        double[] r = dfs(root.right);
        if (l[1] > 0) res = Math.max(l[0] / l[1], res);
        if (r[1] > 0) res = Math.max(r[0] / r[1], res);
        return new double[]{l[0] + r[0] + root.val, l[1] + r[1] + 1};
    }
}
```

